# 🧠 Нейросеть для анализа текста - Хомлины

Эта ветка содержит полноценную нейросетевую систему для анализа текстов на русском языке.

## 🚀 Быстрый старт

### Windows
Просто запустите:
```bash
start-backend.bat
```

### Linux/Mac
```bash
chmod +x start-backend.sh
./start-backend.sh
```

## 📦 Что включено

### Backend (Python + PyTorch)
- ✅ Нейросеть на базе RuBERT-tiny2
- ✅ Анализ качества текста
- ✅ Определение токсичности
- ✅ REST API на Flask
- ✅ CORS поддержка

### Frontend (JavaScript)
- ✅ Интеграция с backend
- ✅ Fallback на локальный анализ
- ✅ Красивое отображение результатов
- ✅ Комбинированная оценка

## 📁 Структура проекта

```
homlins/
├── backend/
│   ├── models/
│   │   ├── __init__.py
│   │   └── text_analyzer.py      # Нейросетевая модель
│   ├── app.py                     # Flask сервер
│   ├── requirements.txt           # Зависимости Python
│   ├── .gitignore
│   └── README.md
├── ai-checker-backend.js          # Frontend для работы с backend
├── start-backend.bat              # Запуск для Windows
├── start-backend.sh               # Запуск для Linux/Mac
├── INTEGRATION.md                 # Инструкция по интеграции
└── [остальные файлы сайта]
```

## 🔧 Установка вручную

1. **Установите Python 3.8+**

2. **Перейдите в папку backend:**
```bash
cd backend
```

3. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

4. **Активируйте его:**
- Windows: `venv\Scripts\activate`
- Linux/Mac: `source venv/bin/activate`

5. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

6. **Запустите сервер:**
```bash
python app.py
```

## 🌐 Интеграция с сайтом

В файле `fanfics.html` замените:
```html
<script src="ai-checker.js"></script>
```

На:
```html
<script src="ai-checker-backend.js"></script>
```

## 📊 Как работает нейросеть

```
Текст пользователя
    ↓
Токенизация (разбивка на токены)
    ↓
RuBERT-tiny2 (понимание контекста)
    ↓
┌─────────────────┬──────────────────┐
│  Голова качества │ Голова токсичности│
└─────────────────┴──────────────────┘
    ↓                    ↓
Оценка качества    Оценка токсичности
    ↓                    ↓
    └────────┬───────────┘
             ↓
    Итоговая оценка
```

## 🎯 Возможности

- **Структурный анализ**: количество предложений, слов, прилагательных
- **Нейросетевой анализ**: глубокое понимание текста через RuBERT
- **Оценка качества**: насколько хорошо написан текст
- **Детекция токсичности**: проверка на неприемлемый контент
- **Комбинированная оценка**: структура + нейросеть = точный результат

## 📡 API

### POST /api/analyze
Анализирует текст

**Запрос:**
```json
{
  "text": "Ваш текст здесь",
  "character": "Карл"
}
```

**Ответ:**
```json
{
  "score": 85,
  "structure": {...},
  "neural": {
    "quality_score": 90.5,
    "toxicity_score": 5.2,
    "is_appropriate": true
  }
}
```

### GET /api/health
Проверка работы сервера

## 💡 Требования

- Python 3.8 или выше
- 2GB RAM (минимум)
- Интернет (для первой загрузки модели)

## 🐛 Решение проблем

**Backend не запускается?**
- Проверьте версию Python: `python --version`
- Убедитесь, что виртуальное окружение активировано

**CORS ошибка?**
- Убедитесь, что backend запущен
- Проверьте, что flask-cors установлен

**Медленная работа?**
- Первый запрос медленный (загрузка модели)
- Последующие запросы будут быстрее

## 📝 Дополнительная информация

- `backend/README.md` - детали о backend
- `INTEGRATION.md` - подробная инструкция по интеграции

## 🌿 Git ветки

- `main` - основная ветка с сайтом
- `neural-network-backend` - эта ветка с нейросетью

## 📄 Лицензия

Проект создан для образовательных целей.
